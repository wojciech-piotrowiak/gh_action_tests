name: build

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::group::default"
      - run: echo 'test1'
      - run: echo 'test2'
      - run: echo "::endgroup::"
      
  
